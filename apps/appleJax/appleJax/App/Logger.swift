import os

let logger = Logger(subsystem: "com.joshpointer.applejax", category: "general")
let renderLogger = Logger(subsystem: "com.joshpointer.applejax", category: "render")
let audioLogger = Logger(subsystem: "com.joshpointer.applejax", category: "audio")
